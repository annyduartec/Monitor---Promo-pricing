import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allow fetching from Google Sheets from API routes
  // No external image domains needed for this app
};

export default nextConfig;
